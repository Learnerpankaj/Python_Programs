class prime:
    def checkprime(self,num):
        for i in range(2,num-1):
            if num %i ==0:
                print("Not a Prime number")
                break
            else:
                print("Prime number")
p = prime()
p.checkprime(10)
